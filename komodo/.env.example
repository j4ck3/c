# Komodo - copy to .env and set for production
# Generate a random secret: openssl rand -base64 32

KOMODO_JWT_SECRET=change-me-in-production

# Optional: create first admin on startup (only when no users exist)
# KOMODO_INIT_ADMIN_USERNAME=admin
# KOMODO_INIT_ADMIN_PASSWORD=your-secure-password

# Logging: debug (auth errors in logs) or info
# KOMODO_LOGGING_LEVEL=info
